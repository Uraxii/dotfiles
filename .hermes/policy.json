{
  "_note": "Ported from .claude/settings.json permissions. Regex patterns checked against terminal-tool command string. deny always wins. Empty allow = anything-not-denied is permitted.",
  "deny": [
    "^git reset --hard",
    "^git push --force",
    "^git push -f",
    "^git clean",
    "^rm -rf /"
  ],
  "allow": []
}
