include consts
include prefs
include themes/$theme/*

exec_always ~/.config/sway/scripts/set-theme.sh $theme $font_family

include modules/*



# Default config for sway in /etc/sway/config
#
# Copy it to ~/.config/sway/config to restore defaults.
#
# Read `man 5 sway` for a complete reference.

include /etc/sway/config.d/*

